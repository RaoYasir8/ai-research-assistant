use_default_settings: true

general:
  debug: false
  instance_name: "Research Search"

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  secret_key: "overridden-by-SEARXNG_SECRET"
  limiter: false
  image_proxy: false

ui:
  static_use_hash: true
